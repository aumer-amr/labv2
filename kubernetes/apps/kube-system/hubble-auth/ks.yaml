---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: hubble-auth
spec:
  dependsOn:
    - name: cilium
    - name: onepassword-connect
      namespace: external-secrets
    - name: envoy-gateway
      namespace: network
    - name: kanidm
      namespace: security
  interval: 1h
  path: ./kubernetes/apps/kube-system/hubble-auth/app
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: kube-system
  wait: true
